<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Needed for Android 13+; harmless on older versions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <application>
        <!-- NotificationListenerService for capturing notifications during Landline mode -->
        <service
            android:name=".LandlineNotificationListenerService"
            android:label="Landline Notification Logger"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>
</manifest>