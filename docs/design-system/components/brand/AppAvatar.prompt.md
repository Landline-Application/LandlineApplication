A round monogram chip for an app or sender. Tint is hashed from the name so each app stays consistent.

```jsx
<AppAvatar name="WhatsApp" />
<AppAvatar name="Gmail" size={40} />
```

Leads each app group in the notification feed.
