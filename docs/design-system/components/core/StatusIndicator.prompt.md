The signature "session is live" light — a dot that pulses a moss glow when active.

```jsx
<StatusIndicator active label="Active" />
<StatusIndicator active={false} label="Inactive" size="sm" />
```

Sits at the center of the rotary dial and in status rows. Inactive renders a dim grey dot with no animation.
