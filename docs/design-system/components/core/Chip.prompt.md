A pill-shaped filter toggle. Outlined when idle, solid moss when active.

```jsx
<Chip label="All" active />
<Chip label="Messages" onClick={() => setFilter('Messages')} />
```

Use in horizontal scrolling filter rows. Drives the notification feed's app/category filters.
