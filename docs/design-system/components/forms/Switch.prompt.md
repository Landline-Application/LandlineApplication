Toggle switch — moss track when on, stone when off; paper thumb glides over 300ms.

```jsx
<Switch checked={autoReply} onChange={setAutoReply} />
```

Use for settings rows (auto-reply, per-app bypass, etc.).
