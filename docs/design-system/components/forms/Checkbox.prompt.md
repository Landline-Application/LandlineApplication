Rounded-square checkbox — moss fill with a white check when selected.

```jsx
<Checkbox checked={agreed} onChange={setAgreed} label="I agree to the Terms & Privacy Policy" />
```

Used in the terms-acceptance gate. Optional inline `label`.
